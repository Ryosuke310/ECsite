<p>このメールは <%= @email %> 宛に送信されています。</p>

<p>リンクをクリックして本登録を完了してください。</p>

<p><%= link_to 'Confirm my account', user_confirmation_url + "?confirmation_token=#{@token}" %></p>

<% if false %>

<p><%= link_to 'Confirm my account', user_confirmation_url(@resource, confirmation_token: @token) %></p>

<% end %>